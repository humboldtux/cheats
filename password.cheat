% password

;https://github.com/HashPals/Search-That-Hash

# Search That Hash basic
sth -t <hash>

# Search That Hash basic with file
sth -f <file>

# basic john the ripper
john <hashfile> --wordlist=<list>
$ list: find /opt/wordlists -type f -not -path "/opt/wordlists/SecLists/.git/*" -print
