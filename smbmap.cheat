% smb

# Basic unauth connection
smbmap -H <ip>

# List SMB shares with invalid user + empty password
smbmap -u invalid -H <target>

# List shares with valid user
smbmap -u <user> -p <pwd> -d <domain> -H <target>
